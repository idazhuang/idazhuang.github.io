# 补充 02 · 遥感基础 · 名词逐个讲透
## 原 M0《遥感基础知识入门》给的是名词表，这一页给的是"真的懂了"

> **这一页解决什么**：原 M0 第 11 节的术语表是**一句话一个词**——`speckle：相干斑噪声`。对已经懂的人是提醒，对没懂的人等于没说。这一页把关键词按固定六段展开，每个词都告诉你**它到底在发生什么、在图上长什么样、不懂会在哪卡住、想深入去哪看**。
>
> **每个词的固定六段**：
> ① 一句话是什么　② 打个比方　③ 底层在发生什么（该上公式就上）　④ 在图上/代码里长什么样　⑤ 不懂它会在哪卡住　⑥ 想深入去哪看
>
> **怎么用这一页**：**不要一次读完。** 读 §1（speckle，写得最透，是样板）+ §2（成像几何四件套），其余当字典，遇到再查。§7 有一张"什么时候该回来查哪个词"的索引。
>
> **配套**：具体资料的获取方式见《学习资料地图》那一页。

---

# 1. speckle（相干斑）—— 这一节写透，其余的词照这个标准理解

你反馈说"看到 speckle 完全不知道是什么"。它确实是 SAR 里第一个真正的门槛。这一节按六段全展开。

## ① 一句话是什么

**speckle 是 SAR 图像上那层洗不掉的颗粒感。它不是设备噪声，是相干成像原理的必然产物——所以你不可能靠"换个更好的传感器"消除它。**

中文叫"相干斑"。注意**不是"斑点噪声"**：叫它噪声容易让人以为是随机干扰，但它其实是**信号本身的一部分**，携带着地表微观结构的信息。

## ② 打个比方

拿一支激光笔照白墙，你会看到墙上有一层闪烁的、颗粒状的亮暗花纹——这就是光学里的 speckle，和 SAR 里的是同一个物理现象。

**为什么会有这层花纹？** 墙面在微观上是粗糙的。激光是**相干光**（所有波步调一致），从墙上无数个微小凸起反射回来的波，走过的路程长短不一，回到你眼睛时**有的波峰对波峰（增强），有的波峰对波谷（抵消）**。于是明明均匀的白墙，看起来却坑坑洼洼。

**换成手电筒就没有这个现象**——因为手电筒的光不相干，各个波之间没有固定的相位关系，叠加时只是能量相加，不会互相抵消。

**SAR 就是"用微波当激光笔照地球"**。所以：

> **相干光源 + 粗糙表面 = 必然出现 speckle。** 光学卫星用的是太阳光（非相干），所以光学影像上没有 speckle。**这就是"SAR 图和照片长得不一样"的头号原因。**

## ③ 底层在发生什么（这一段决定你能不能真懂）

**第一步：一个像素里装了什么。**

SAR 的一个分辨单元（比如 Sentinel-1 大约 10 m × 10 m）里，有**成千上万个**小散射体——草叶、石子、屋顶的每一小块。雷达发出一个脉冲，这些散射体各自把波弹回来。传感器收到的是它们的**复数相干叠加**：

```text
z = Σ(k=1..N)  A_k · exp(j·φ_k)
```

其中 `A_k` 是第 k 个散射体的回波幅度，`φ_k` 是它的相位（由它到卫星的距离决定）。

**第二步：为什么结果是随机的。**

`φ_k` 取决于距离，而**距离的微小差别（半个波长，几厘米）就会让相位转一整圈**。地表这些散射体的位置在厘米尺度上完全无规律，所以 `φ_k` 实际上是 `[0, 2π)` 上均匀分布的随机数。

于是 `z` 就是**几万个随机方向的小矢量首尾相接**——数学上叫**二维随机游走**。由中心极限定理，`z` 的实部和虚部各自趋于零均值高斯分布。由此可以推出：

- **幅度** `A = |z|` 服从 **Rayleigh 分布**
- **强度** `I = |z|²` 服从**指数分布**

**第三步：为什么说它是"乘性"的。**

强度的指数分布有一个要命的性质：**均值和标准差相等**。写成模型就是

```text
I = R × n        其中 E[n] = 1, Var[n] = 1
```

其中 `R` 是地表真实的后向散射系数（你想要的信号），`n` 是 speckle。

- **加性噪声**（普通照片的噪声）：`I = R + n`，噪声强度和信号亮暗**无关**——暗处和亮处噪声一样大。
- **乘性噪声**（speckle）：**信号越强，噪声的绝对波动越大**。`Std[I] = R`，正比于信号本身。

**这就是为什么"通用的高斯去噪对 SAR 效果差"**——高斯去噪的整套假设（噪声与信号独立、方差恒定）在这里全部不成立。

**第四步：多视（multi-look）和等效视数（ENL）。**

既然单次测量的方差这么大，那就**多测几次求平均**。把 `L` 个独立观测（不同的子孔径，或空间上相邻的像素）平均：

```text
I_L = (1/L) · Σ(i=1..L) I_i
```

`I_L` 服从 **Gamma 分布**，其**变异系数**（标准差 ÷ 均值）从 1 降到 `1/√L`。

- `L=1`（单视）：变异系数 = 1，噪得一塌糊涂
- `L=4`：变异系数 = 0.5
- `L=16`：变异系数 = 0.25

**代价是分辨率变差**——空间多视等于把 `L` 个像素合成一个。**这是 SAR 里最基本的一组权衡：噪声 ↔ 分辨率。**

**ENL（Equivalent Number of Looks，等效视数）** 就是反过来量这件事：在一块**均质区域**（比如一片平静水面）上算

```text
ENL = (μ / σ)²        μ = 该区域均值, σ = 该区域标准差
```

ENL 越大 = 图越平滑。**它是评价去斑算法好坏的标准指标之一**——你以后写论文做去斑实验，表格里一定有它。

## ④ 在图上 / 代码里长什么样

**在图上**：一片均匀的农田，光学影像上是一块平整的绿色；SAR 上是**满屏跳动的黑白麻点**。同一块地，相邻两个像素的值可能差三倍。

**在代码里 —— 15 分钟亲手做一遍（不用下载任何数据）**

光看公式记不住。下面这段代码用 numpy 仿真出 speckle，然后**量出它确实是乘性的**。建议现在就跑一遍，存进 `RS-CV/00-基础理论/`。

```python
import numpy as np

rng = np.random.default_rng(0)
H = W = 512

# 造一块"真实地表"：三种不同后向散射强度的区域
truth = np.full((H, W), 0.1)      # 暗区，比如平静水面
truth[100:400, 100:250] = 0.5     # 中等，比如农田
truth[150:350, 300:450] = 1.0     # 亮区，比如城区

def simulate_speckle(R, looks=1, rng=rng):
    """按 §3 的物理过程仿真：复数高斯 -> 取模平方 -> 多视平均"""
    acc = np.zeros_like(R)
    for _ in range(looks):
        re = rng.normal(0, np.sqrt(R / 2))   # 实部
        im = rng.normal(0, np.sqrt(R / 2))   # 虚部
        acc += re**2 + im**2                  # 强度 = |z|^2
    return acc / looks

I1 = simulate_speckle(truth, looks=1)   # 单视
I4 = simulate_speckle(truth, looks=4)   # 四视

# 验证一：单视强度的 均值 == 标准差（变异系数 = 1）
for name, I in [("1-look", I1), ("4-look", I4)]:
    for v in (0.1, 0.5, 1.0):
        m = np.isclose(truth, v)
        mu, sd = I[m].mean(), I[m].std()
        print(f"{name} R={v}: mean={mu:.3f} std={sd:.3f} "
              f"CV={sd/mu:.3f} ENL={(mu/sd)**2:.2f}")

# 验证二：局部标准差 与 局部均值 成正比 —— 这就是"乘性"的直接证据
from numpy.lib.stride_tricks import sliding_window_view
w = sliding_window_view(I1, (9, 9))
local_mean = w.mean(axis=(-1, -2)).ravel()
local_std  = w.std(axis=(-1, -2)).ravel()
print("corr(局部均值, 局部标准差) =", round(float(np.corrcoef(local_mean, local_std)[0, 1]), 3))
```

**你应该看到的输出**（数字会因随机种子略有出入）：

```text
1-look R=0.1: mean=0.100 std=0.100 CV=1.003 ENL=0.99
1-look R=0.5: mean=0.504 std=0.503 CV=0.998 ENL=1.00
1-look R=1.0: mean=1.003 std=0.992 CV=0.990 ENL=1.02
4-look R=0.1: mean=0.100 std=0.050 CV=0.499 ENL=4.01
4-look R=0.5: mean=0.499 std=0.251 CV=0.503 ENL=3.95
4-look R=1.0: mean=0.998 std=0.496 CV=0.498 ENL=4.04

corr(局部均值, 局部标准差) = 0.982
```

**这五行输出就是整节理论的证明**：

- 单视 CV ≈ **1.00**，且三种亮度都是 1.00 → **噪声强度随信号成比例放大 = 乘性**（若是加性，暗区 CV 会远大于亮区）。
- 四视 CV ≈ **0.50** = `1/√4` → 多视降噪的公式是对的。
- ENL 算出来 ≈ 视数本身 → 这个指标名副其实。
- 局部均值与局部标准差**相关系数 0.98** → 亮的地方一定更"跳"。**这条就是所有经典去斑滤波器（Lee、Kuan、Frost）的设计出发点。**

> **加一句 15 秒的对照实验**：把 `simulate_speckle` 换成 `truth + rng.normal(0, 0.2, truth.shape)`（加性高斯），再跑一次相关系数——你会看到它掉到接近 0。**亲手看到这个对比，speckle 是什么你就永远忘不掉了。**

## ⑤ 不懂它会在哪卡住

| 场景 | 不懂 speckle 会怎样 |
|---|---|
| 读 SAR 论文 | Introduction 第一段几乎必提 speckle，看不懂就抓不住这篇在解决什么 |
| 做 SAR 目标检测 | 想不通"为什么虚警这么多"——小船和一撮亮 speckle 在单个像素上没法区分 |
| 直接套用光学的数据增强 | 高斯噪声增强、直方图均衡在 SAR 上可能是负作用 |
| 看到 CFAR 这个词 | CFAR（恒虚警率检测）整套东西就是为了对付 speckle 统计特性而生的，不懂前者就不懂后者 |
| 选题 | SAR 方向一大半的可做题目都直接或间接和它有关 |

## ⑥ 想深入去哪看（按投入时间排序）

| 想要 | 去哪 | 具体看哪 | 花多久 |
|---|---|---|---|
| 建立直觉 | ASF《Introduction to SAR》 `https://hyp3-docs.asf.alaska.edu/guides/introduction_to_sar/` | 整页，有大量配图 | 30 分钟 |
| 系统入门 | NASA SERVIR《The SAR Handbook》（免费 PDF，从 NASA Earthdata 站点下） | 第 2 章 SAR 基础 | 2 小时 |
| 看视频 | NASA ARSET "Introduction to SAR" 系列（免费，官网可回放，有 PDF 讲义） | 第 1、2 讲 | 3 小时 |
| **权威深挖** | **Oliver & Quegan《Understanding Synthetic Aperture Radar Images》** | **第 4–5 章（speckle 统计与滤波）——这是 speckle 的圣经** | 一周 |
| 追最新方法 | Google Scholar 搜 `SAR despeckling survey` / `self-supervised despeckling` | 近三年综述各一篇 | 半天 |

## ★ 这个词能引出什么选题（你未来的矿）

原 M0 只说"可做针对乘性噪声的鲁棒方法"。具体化一下：

1. **去斑与检测的联合优化**：绝大多数流程是"先去斑再检测"，但去斑会抹掉小目标。**联合训练**、或者干脆**让检测器直接吃带斑图像**，哪个更好？这是个能做实验说清楚的问题。
2. **自监督去斑**：SAR 没有干净的真值图。近年 speckle2void、MERLIN 这类方法用"同一场景的两次观测"或"复数数据的实部虚部互为噪声副本"来绕开真值——**思路可迁移到检测的自监督预训练**。
3. **评测本身的问题**：很多去斑论文在合成数据（就像上面那段代码）上刷 PSNR，但真实 SAR 上表现另说。**做一个更诚实的评测基准**，是低风险高引用的活。
4. **乘性噪声下的数据增强**：CV 里的增强都是加性思维。**乘性场景该怎么增强**？这是个小而具体、适合当第一篇的口子。

> **记进知识库**：现在就在 `RS-CV/00-基础理论/术语卡片.md` 建第一张卡：词条、一句话定义、一个比方、一条公式、一句"它引出的研究问题"。**这一页里每个词都值得这样一张卡。**

---

# 2. 成像几何四件套（看懂 SAR 图为什么"歪"）

这四个词永远一起出现，一起理解最省力。**根源都是同一件事：SAR 是侧着看的，而且它量的是"距离"不是"角度"。**

## 2.1 距离向 / 方位向（range / azimuth）

- **① 是什么**：SAR 图像的两个轴。**距离向** = 垂直于卫星飞行方向（雷达波打出去的方向）；**方位向** = 平行于飞行方向。
- **② 比方**：卫星像一个人拿手电筒**侧着**照地面走路。手电筒光扫出去的那条线是距离向，人走路的方向是方位向。
- **③ 底层**：两个方向的分辨率来源**完全不同**。距离向靠脉冲的带宽（脉冲越短越准）；方位向靠"合成孔径"——用飞行过程中连续采集的回波，合成出一个等效的超大天线。
- **④ 长什么样**：SAR 原始图的两个轴不是"东西/南北"，而是"距离/方位"。做成正射产品后才转成地理坐标。
- **⑤ 卡在哪**：读 SAR 论文时 `range direction` / `azimuth resolution` 满篇都是；不懂就不知道作者在说图的哪个方向。
- **⑥ 去哪看**：ASF《Introduction to SAR》"SAR Geometry" 一节；Cumming & Wong《Digital Processing of SAR Data》第 1 章（**只读第 1 章，后面是成像算法，你暂时用不上**）。

## 2.2 叠掩 / 阴影 / 透视收缩（layover / shadow / foreshortening）

- **① 是什么**：侧视成像导致的三种几何畸变。**光学影像上不存在这三样东西**。
- **② 比方**：太阳低角度照一排楼，地上有长影子（shadow）；而 SAR 更奇怪——**山顶的回波比山脚先回来**，于是山顶被"画"到了山脚前面（layover），整座山看起来朝卫星方向倒下去。
- **③ 底层**：SAR 按**回波时间**排像素，时间对应**斜距**。高处离卫星更近 → 回波更早 → 排在更前的位置。坡度越陡越严重，坡度大于入射角时就发生 layover。背面照不到的地方没有回波 → 全黑 = shadow。
- **④ 长什么样**：山区 SAR 影像上，朝向卫星的坡被"压扁"且异常亮，背坡是一条纯黑带。城市里高楼也会 layover 到街道上。
- **⑤ 卡在哪**：做山区/城区目标检测时，**这些畸变会被模型当成"目标"或"遮挡"**；也解释了为什么 SAR 检测数据集偏爱海面船只——海面平坦，没这些麻烦。
- **⑥ 去哪看**：加拿大自然资源部（NRCan）的《Microwave Remote Sensing》在线教程，图画得最直观；ASF 那页也有专门一节。

## 2.3 入射角（incidence angle）

- **① 是什么**：雷达波打到地面时，与地面法线的夹角。Sentinel-1 大约 29°–46°。
- **② 比方**：同一块草坪，正午直射和傍晚斜射，看起来亮度完全不同。
- **③ 底层**：后向散射强度**强烈依赖入射角**。同一目标、不同入射角，图上亮度可以差好几 dB。这也是为什么 SAR 数据必须做**辐射定标**（见 §3.3）才能跨影像比较。
- **④ 长什么样**：一景影像内，近距端（入射角小）整体偏亮，远距端偏暗，有肉眼可见的亮度梯度。
- **⑤ 卡在哪**：不同轨道、不同入射角的数据混在一起训练，模型会学到"亮度"这个伪特征而不是目标本身，**换个轨道就掉点**——这是 SAR 深度学习一个常见但容易被忽略的域偏移来源，**本身就是个选题**。
- **⑥ 去哪看**：《The SAR Handbook》第 2 章的 radiometric terrain correction 部分。

## 2.4 后向散射（backscatter）与散射机制

- **① 是什么**：雷达波打到地物后**返回卫星那一部分**的能量。SAR 图像的像素值本质上就是它。
- **② 比方**：对着不同材质的墙喊话，听回声大小。光滑玻璃幕墙把声音弹到别处（回声小），砖墙粗糙（回声大），墙角则把声音原路弹回（回声最大）。
- **③ 底层**：三种主要机制——
  - **面散射**：粗糙地面。粗糙度相对波长越大，回波越强。**平静水面几乎全反射走了，所以水在 SAR 上是黑的**（这个特性让 SAR 做洪水监测特别好用）。
  - **体散射**：植被冠层，波在枝叶间多次弹射，回波中等且杂乱。
  - **二面角散射**（double bounce）：地面 → 墙面 → 回卫星，**能量几乎原路返回，回波极强**。城市和船只在 SAR 上贼亮就是因为这个。
- **④ 长什么样**：水最黑 → 平地/农田偏暗 → 森林中等 → 城市/金属目标极亮。**记住这个亮度顺序，你就能粗读一张 SAR 图了。**
- **⑤ 卡在哪**：不懂这个就不理解"为什么 SAR 上船那么亮、检测应该好做"，也不理解为什么强散射体旁边会出现十字形的**旁瓣**假目标。
- **⑥ 去哪看**：ASF《Introduction to SAR》的 scattering mechanisms 那几张图，五分钟看懂。

---

# 3. SAR 数据形态（下载下来那堆文件都是什么）

## 3.1 SLC / GRD（单视复数 / 地距多视）

- **① 是什么**：同一次观测的两种产品级别。**SLC** = Single Look Complex，保留**复数**（幅度 + 相位）；**GRD** = Ground Range Detected，只保留幅度，已做多视和地距投影。
- **② 比方**：SLC 是相机的 RAW，GRD 是导出的 JPEG。RAW 信息全但难处理，JPEG 直接能看。
- **③ 底层**：相位信息只在 SLC 里。**做 InSAR（测形变/高程）必须用 SLC；做检测/分类通常 GRD 就够。**
- **④ 长什么样**：SLC 每个像素是 `complex64`；GRD 是实数强度图。SLC 像素还不是正方形的（距离向和方位向采样间隔不同）。
- **⑤ 卡在哪**：下错产品级别，几十 GB 白下。**W1-D5 下数据集之前先确认你要哪一级。**
- **⑥ 去哪看**：ESA Copernicus 官方的 Sentinel-1 产品说明页；ASF Vertex 下载界面上每种产品都有说明。

## 3.2 幅度 / 强度 / dB

- **① 是什么**：幅度 `A`、强度 `I = A²`、分贝 `dB = 10·log10(I)`。
- **② 比方**：和声音的"振幅 / 功率 / 分贝"是同一套换算。
- **③ 底层**：SAR 的动态范围极大（水面和城市能差好几个数量级）。**线性显示的话，除了几个亮点全是黑的**。取对数把动态范围压回来。而且 §1 说过，**log 变换还有个隐藏好处：把乘性噪声变成加性噪声**（`log(R·n) = log R + log n`）——很多经典去斑算法就是先取 log 再当加性问题处理。
- **④ 长什么样**：
  ```python
  img_db = 10 * np.log10(intensity + 1e-10)   # +eps 防 log(0)
  # 再做 2%–98% 分位数拉伸才能看
  lo, hi = np.percentile(img_db, [2, 98])
  vis = np.clip((img_db - lo) / (hi - lo), 0, 1)
  ```
- **⑤ 卡在哪**：不做 log + 拉伸就直接 `plt.imshow`，你会得到一张几乎全黑的图，然后以为数据坏了。**这是 SAR 新手第一天必踩的坑。**
- **⑥ 去哪看**：《The SAR Handbook》的教程章节，每个都是从这一步开始的。

## 3.3 辐射定标 / σ⁰ / γ⁰

- **① 是什么**：把传感器记的原始数字（DN）转换成**有物理意义、可跨影像比较**的后向散射系数。σ⁰（sigma nought）是最常用的那个。
- **② 比方**：相机的 RAW 值只在同一组曝光参数下可比；定标就是把它换算成绝对亮度单位。
- **③ 底层**：要补偿距离衰减、天线方向图、入射角等。γ⁰ 相对 σ⁰ 进一步归一化了入射角影响，**在地形起伏区更稳定**。
- **④ 长什么样**：定标后典型值范围约 −25 dB（水）到 +5 dB（城市）。看到论文里写"−15 dB 的农田"你就知道量级对不对。
- **⑤ 卡在哪**：**混用未定标数据做训练/测试，跨场景直接崩**。这也是很多 SAR 深度学习论文复现不出来的隐藏原因之一。
- **⑥ 去哪看**：ESA SNAP 软件的 Radiometric Calibration 操作文档；NASA ARSET 的 SAR 系列讲义。

## 3.4 极化（polarization）HH / HV / VH / VV

- **① 是什么**：雷达波的电场振动方向。第一个字母 = 发射极化，第二个 = 接收极化。HH = 水平发、水平收；HV = 水平发、垂直收（交叉极化）。
- **② 比方**：戴偏振墨镜看水面。转 90° 反光就变了——同一个场景，换个"振动方向"看到的信息不同。
- **③ 底层**：不同散射机制对极化的响应不同。**面散射保持极化（同极化强）；体散射会让极化旋转（交叉极化强）**。所以 HV 通道对植被特别敏感，是森林制图的主力。
- **④ 长什么样**：Sentinel-1 常见 VV+VH 双极化。可以把 VV、VH、VV/VH 塞进 RGB 三通道做假彩色，城市/植被/水一眼分开。
- **⑤ 卡在哪**：**别把多极化当成光学的多光谱来处理**——物理机理完全不同，直接套多光谱的融合方法往往没道理。原 M0 提到的"极化感知的轻量利用"就是这个口子。
- **⑥ 去哪看**：入门看《The SAR Handbook》极化章节；深入看 Lee & Pottier《Polarimetric Radar Imaging》（**很硬，等你真的要做极化再读**）。

## 3.5 InSAR / 相干性（coherence）

- **① 是什么**：用同一地区**两次观测的相位差**来测地表高程或形变。相干性是衡量这两次观测"可比程度"的指标（0–1）。
- **② 比方**：两次用同一把尺子量同一段距离，差值告诉你它变没变。相干性就是"这两次量得靠不靠谱"。
- **③ 底层**：相位差包含地形项和形变项，能测到**厘米甚至毫米级**的地面沉降。植被区因为叶子在动，两次观测的散射体变了，相干性低，测不准（**时间失相干**）。
- **④ 长什么样**：干涉图上是彩色的同心条纹，一圈 = 半个波长的形变。
- **⑤ 卡在哪**：**这是空天院（王超组等）的核心方向之一**。你不一定做 InSAR，但要能听懂他们在说什么。
- **⑥ 去哪看**：ASF 的 InSAR 教程系列（有 Jupyter Notebook 可跑）；ESA SNAP 的 InSAR 教程。

## 3.6 方位角敏感（azimuth sensitivity）

- **① 是什么**：同一个目标，从不同方位照射，SAR 上的"长相"差异巨大。
- **② 比方**：同一艘船，正侧面照和斜 45° 照，反射面的朝向完全变了。光学照片只是视角变了，SAR 则是**回波强度整个重排**。
- **③ 底层**：强散射来自那些恰好垂直于雷达视线的面。船体、集装箱的边棱朝向一变，亮点分布就换一套。
- **④ 长什么样**：同一艘船在两个轨道的影像上，可能一次是清晰的亮长条，一次是几个散开的亮点。
- **⑤ 卡在哪**：**深度学习模型很容易过拟合某个方位角下的外观**，换轨道就掉点。原 M0 说的"方位角不变表示"就是冲这个去的。
- **⑥ 去哪看**：Google Scholar 搜 `SAR ship detection azimuth angle robustness`，近两年的论文 Introduction 里都会讲这个动机。

---

# 4. 光学侧要补的几个（原 M0 讲过但太简）

## 4.1 GSD / 空间分辨率

- **一句话**：一个像素对应地面多少米。Sentinel-2 是 10 m，高分二号约 1 m，DOTA 里的图从 0.1 m 到几米不等。
- **底层与卡点**：**GSD 决定了"小目标"到底有多小**。一辆车 4 m 长，在 10 m GSD 上不到半个像素——**根本检测不了**；在 0.5 m 上是 8 像素，勉强能做。**读任何遥感检测论文，先找它用的 GSD，否则"小目标检测"这四个字没有意义。**
- **去哪看**：原 M0 第 5 节已经讲了四种分辨率，够用。补充：DOTA 论文（W1-D5 要读）里有每张图的 GSD 说明。

## 4.2 大气校正 / 几何（正射）校正

- **一句话**：辐射校正去掉大气对光谱的干扰；几何校正把因地形起伏和视角造成的位置偏移拉正。
- **卡点**：**CV 里完全没有这两步**，所以初学者会奇怪"为什么遥感数据处理这么麻烦"。答案是：遥感要的是**可跨时间、跨传感器比较的物理量**，不是"好看的图"。
- **去哪看**：中国大学 MOOC《遥感原理与应用》里对应的两讲；韦玉春《遥感数据处理方法》第 3 章（**原手册 W1-D7 就安排了这一章，正好衔接**）。

## 4.3 DN / 反射率 / NDVI

- **一句话**：DN 是传感器记的原始整数；转成反射率才有物理意义；NDVI =(NIR−Red)/(NIR+Red) 是最经典的植被指数。
- **卡点**：直接拿 DN 算 NDVI 跨影像比较是错的。
- **去哪看**：任一本遥感导论教材的"辐射定标"一节。

---

# 5. 旋转框检测专有名词（W1-D6 写脚本前必须懂）

这几个词原手册 W1-D6 会让你写解析脚本，但没提前解释，**先在这里垫上**。

## 5.1 旋转框的三种角度定义：oc / le90 / le135

- **① 是什么**：同一个斜框，学术界有三套编码方式。MMRotate 的 config 文件名里那个 `le90` 就是它。
- **② 比方**：同一个方向，有人说"东偏北 30°"，有人说"北偏东 60°"——描述的是同一件事，数值不同。
- **③ 底层**：旋转框用 `(x, y, w, h, θ)` 表示。分歧在于 `θ` 的取值范围和"哪条边算 w"：
  - **oc**（OpenCV 定义）：`θ ∈ (0°, 90°]`
  - **le90**（long edge 90）：**长边**为 w，`θ ∈ [-90°, 90°)`
  - **le135**：`θ ∈ [-45°, 135°)`
- **④ 长什么样**：`oriented_rcnn_r50_fpn_1x_dota_le90.py` —— 文件名里就写着它用哪套。
- **⑤ 卡在哪**：**用 le90 的权重配 le135 的 config，框会全歪**，而且不报错，你会以为模型坏了。W1-D6 写可视化脚本时，如果框画得莫名其妙，先怀疑角度定义。
- **⑥ 去哪看**：MMRotate 文档的 "Angle Version" 一节；Oriented R-CNN 论文（W2-D8 要读）里也有讨论。

## 5.2 角度周期性问题（boundary discontinuity）

- **① 是什么**：`-89°` 和 `+89°` 在几何上几乎是同一个方向，但数值上差 178°。直接用 L1 loss 回归角度，模型在边界处会收到巨大的错误梯度。
- **② 比方**：钟表上 11 点和 1 点只差两小时，但数字上差 10。
- **③ 底层**：**这是旋转框检测最经典的一个"坑"，也是最高产的一个选题方向**——GWD、KLD、CSL 等一系列有影响力的工作都在解决它。
- **⑤ 卡在哪**：不懂这个，你读 W2 的方法论文时会完全不理解"他们为什么要把框建模成高斯分布"。
- **⑥ 去哪看**：Google Scholar 搜 `rotated object detection boundary discontinuity`，找 GWD / KLD 两篇。**这是你 M3 选方向时的重要候选。**

## 5.3 mAP：VOC07 / VOC12 两种算法

- **一句话**：DOTA 榜单历史上常用 **VOC07 的 11 点插值**，而 COCO 系用连续积分。**同一个模型两种算法能差 1–2 个点。**
- **卡在哪**：**复现不出论文数字时，先查是不是 mAP 算法不同**，而不是急着改模型。这个坑能省你一整周。
- **去哪看**：MMRotate 的 `eval_map` 实现里有 `use_07_metric` 参数，去代码里看一眼最直观。

---

# 6. 一张速查表：这个词什么时候会回来找你

| 术语 | 原手册第一次真正用到 | 不懂的后果 |
|---|---|---|
| speckle / 乘性噪声 / ENL | M0；SAR 方向选题时全程 | 读不懂 SAR 论文的动机段 |
| 后向散射 / 散射机制 | 读 SAR 数据集论文时 | 不理解目标为什么亮 |
| layover / shadow | 做非海面 SAR 检测时 | 把畸变当目标 |
| SLC / GRD | W1-D5 下数据集 | 下错产品，几十 GB 白费 |
| dB / log 拉伸 | 第一次可视化 SAR 图 | 得到全黑图以为数据坏了 |
| 极化 HH/HV/VV | 用多极化数据时 | 误把它当多光谱处理 |
| GSD | W1-D5 读 DOTA 论文 | "小目标"这个概念失去意义 |
| le90 / le135 | **W1-D6 写旋转框脚本**、**W2 跑 baseline** | 框画歪、权重配错 |
| 角度周期性 | W2-D8 读 Oriented R-CNN | 看不懂方法的核心动机 |
| mAP07 vs mAP12 | W2 复现 baseline 对数字时 | 白改一周模型 |

---

# 7. 读完自测（比原 M0 那份深一档）

1. 为什么光学卫星影像上没有 speckle，而 SAR 上必然有？（要能说到"相干"两个字）
2. 乘性噪声和加性噪声，在"局部标准差 vs 局部均值"的散点图上分别长什么样？
3. 4 视处理后变异系数是多少？代价是什么？
4. ENL 怎么算？它为什么要在均质区域上算？
5. 一张 SAR 图上，水面、农田、森林、城市，按亮度从暗到亮怎么排？各自对应哪种散射机制？
6. SLC 和 GRD 的核心区别是什么？做 InSAR 必须用哪个？
7. 为什么 SAR 图要先取 log 再显示？log 变换对 speckle 还有什么额外好处？
8. le90 和 le135 的区别是什么？用错会出现什么现象、会不会报错？
9. 旋转框的"角度周期性问题"是什么？为什么它是个高产选题方向？
10. 复现论文时 mAP 差了 1.5 个点，你第一个该怀疑什么？

> 答不出来的，回对应小节。**第 1、2、5、8 题答不出来，不要往 W2 走**——那几个是后面天天要用的。
>
> **下一步**：读《学习资料地图》，把这一页提到的资料真正找到、下载好、排进你的日程。
